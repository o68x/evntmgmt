require 'rails_helper'

RSpec.describe "HappyPaths", type: :request do
  describe "GET /happy_paths" do
    pending "works! (now write some real specs)" do
      get happy_paths_path
      expect(response).to have_http_status(200)
    end
  end
end
